import {FC} from 'react';
import Layout from "@/components/layout/Layout";

const About: FC = () => {
    return (
        <Layout>
            about page
        </Layout>
    );
};

export default About;